Options -MultiViews
RewriteEngine On

# Mantem arquivos estaticos e diretorios reais.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Entrega a SPA para que o React resolva as rotas internas.
RewriteRule ^ index.html [L]
